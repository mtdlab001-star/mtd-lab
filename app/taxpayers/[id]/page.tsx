import Link from 'next/link'
import { supabaseAdmin } from '@/lib/supabase-admin'
import TaxpayerSidebar from '@/app/components/TaxpayerSidebar'
import { assessHmrcConnection } from '@/lib/hmrc-connection-status'
import { incomeSourceType, type MtdIncomeSourceType } from '@/lib/mtd-income-source'

export const dynamic = 'force-dynamic'

function fmtDate(value?: string | null) { if (!value) return 'Not available'; const d = new Date(`${value}T00:00:00`); return Number.isNaN(d.getTime()) ? value : d.toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }) }
function taxYearFor(value?: string | null) { if (!value) return null; const [y,m,d] = value.split('-').map(Number); if (!y || !m || !d) return null; const start = m > 4 || (m === 4 && d >= 6) ? y : y - 1; return `${start}/${String(start + 1).slice(-2)}` }
function acceptedSubmissionFor(submissions:any[],obligation:any){return submissions.find((s:any)=>s.status==='submitted'&&s.period_end===obligation.period_end&&(!obligation.business_id||s.business_id===obligation.business_id))}
function obligationStatus(obligation:any,submission?:any){const hmrc=String(obligation.status||'').toLowerCase();if(hmrc==='fulfilled')return {label:'Fulfilled',cls:'statusDone'};if(submission?.status==='submitted'){const submittedAt=new Date(submission.submitted_at||submission.created_at||0);const age=Date.now()-submittedAt.getTime();if(Number.isFinite(age)&&age>=0&&age<1000*60*15)return {label:'Accepted, awaiting HMRC update',cls:'statusDone'};return {label:'Accepted, obligation still open',cls:'statusOpen'}}return {label:obligation.status||'Open',cls:'statusOpen'}}
function obligationIncomeSourceType(businessId?:string|null){const id=String(businessId||'').toUpperCase();if(id.startsWith('XFIS'))return 'foreign-property';if(id.startsWith('XPIS'))return 'uk-property';return 'self-employment'}

export default async function TaxpayerPage({ params, searchParams }: { params: Promise<{id:string}>, searchParams: Promise<Record<string,string|undefined>> }) {
  const { id } = await params; const qs = await searchParams
  let taxpayer:any = { id, display_name: id === 'demo' ? 'HMRC Sandbox Taxpayer' : id, nino:'', mtditid:'' }; let businesses:any[]=[]; let obligations:any[]=[]; let submissions:any[]=[]; let connection:any=null; let syncStatus='Not synced'; let lastSync:string|null=null; let lastSuccessfulSync:string|null=null; let syncError:string|null=null
  try { const db=supabaseAdmin(); const [{data:t},{data:b},{data:o},{data:q},{data:c},{data:s},{data:lastGood}] = await Promise.all([db.from('taxpayers').select('*').eq('id',id).maybeSingle(),db.from('hmrc_businesses').select('*').eq('taxpayer_id',id).order('created_at'),db.from('hmrc_obligations').select('*').eq('taxpayer_id',id).order('due_date',{ascending:true}),db.from('hmrc_quarterly_submissions').select('business_id,period_end,status,submitted_at,created_at').eq('taxpayer_id',id).eq('status','submitted').order('created_at',{ascending:false}),db.from('hmrc_connections').select('access_token,refresh_token,token_expires_at,connected_at,scope').eq('taxpayer_id',id).maybeSingle(),db.from('hmrc_sync_runs').select('status,error_message,completed_at').eq('taxpayer_id',id).order('created_at',{ascending:false}).limit(1).maybeSingle(),db.from('hmrc_sync_runs').select('completed_at').eq('taxpayer_id',id).eq('status','complete').order('created_at',{ascending:false}).limit(1).maybeSingle()]); if(t) taxpayer=t; businesses=b||[]; obligations=o||[]; submissions=q||[]; connection=c; syncStatus=s?.status==='complete'?'Complete':s?.status==='failed'?'Latest sync failed':'Not synced'; lastSync=s?.completed_at||null; syncError=s?.status==='failed'?(s.error_message||'HMRC synchronisation failed'):null; lastSuccessfulSync=lastGood?.completed_at||null } catch {}
  const connectionStatus=assessHmrcConnection(connection)
  const latestError=qs.error||syncError
  const needsMatchingGrant=String(latestError||'').toLowerCase().includes('not authorised')
  const eligibleObligations = obligations.filter(o=>String(o.period_start||'')>='2025-04-06')
  const eligibleOpen = eligibleObligations.filter(o=>String(o.status).toLowerCase()==='open')
  const acceptedPending = eligibleOpen.filter(o=>!!acceptedSubmissionFor(submissions,o))
  const actionableOpen = eligibleOpen.filter(o=>!acceptedSubmissionFor(submissions,o))
  const eligibleYears = Array.from(new Set(eligibleObligations.map(o=>taxYearFor(o.period_start)).filter(Boolean) as string[])).sort().reverse()
  const requestedYear = qs.year || 'all'
  const selectedYear = requestedYear === 'all' || eligibleYears.includes(requestedYear) ? requestedYear : 'all'
  const visibleObligations = selectedYear === 'all' ? eligibleObligations : eligibleObligations.filter(o=>taxYearFor(o.period_start)===selectedYear)
  const groupedObligations = visibleObligations.reduce((map:Map<string,any[]>,obligation:any)=>{const key=obligation.business_id||'not-supplied';const rows=map.get(key)||[];rows.push(obligation);map.set(key,rows);return map},new Map<string,any[]>())
  const obligationGroups = Array.from(groupedObligations.entries()).map(([businessId,rows])=>{const business=businesses.find((b:any)=>b.business_id===businessId);const sourceType=business?incomeSourceType(business):obligationIncomeSourceType(businessId);const label=sourceType==='self-employment'?'Self Assessment':sourceType==='uk-property'?'UK Property':'Foreign Property';const businessName=business?.business_name||null;const open=rows.filter((o:any)=>String(o.status||'').toLowerCase()==='open');const fulfilled=rows.filter((o:any)=>String(o.status||'').toLowerCase()==='fulfilled');const actionable=open.filter((o:any)=>!acceptedSubmissionFor(submissions,o));const groupNextDue=actionable.map((o:any)=>o.due_date).filter(Boolean).sort()[0]||null;return {businessId,label,businessName,sourceType,rows,total:rows.length,open:open.length,fulfilled:fulfilled.length,nextDue:groupNextDue}}).sort((a:any,b:any)=>{const order:Record<string,number>={'self-employment':0,'uk-property':1,'foreign-property':2};return (order[a.sourceType]??10)-(order[b.sourceType]??10)||String(a.businessName||a.businessId).localeCompare(String(b.businessName||b.businessId))})
  const sourceTypes = Array.from(new Set(obligationGroups.map((group:any)=>group.sourceType).filter(Boolean))) as MtdIncomeSourceType[]
  const legacyOnly = obligations.length>0 && eligibleObligations.length===0
  const nextDue = actionableOpen.map(o=>o.due_date).filter(Boolean).sort()[0]
  return <div className="shell"><TaxpayerSidebar taxpayerId={id} active="overview" sourceTypes={sourceTypes}/><main className="main"><div className="top"><div><h1 className="pageTitle">{taxpayer.display_name}</h1><p className="muted">Making Tax Digital taxpayer workspace</p></div><span className={connectionStatus.badgeClass}>{connectionStatus.label}</span></div><div className="cards"><div className="card"><span className="eyebrow">Connection</span><strong>{connectionStatus.usable?'Usable':'Action needed'}</strong><span className="muted">{connectionStatus.expired?'Refresh required':connectionStatus.connected?'HMRC authorisation':'Not connected'}</span></div><div className="card"><span className="eyebrow">Businesses</span><strong>{businesses.length}</strong><span className="muted">Income sources</span></div><div className="card"><span className="eyebrow">Open to submit</span><strong>{actionableOpen.length}</strong><span className="muted">{eligibleOpen.length} HMRC open across sources</span></div><div className="card"><span className="eyebrow">Accepted pending HMRC</span><strong>{acceptedPending.length}</strong><span className="muted">Awaiting obligation refresh</span></div><div className="card"><span className="eyebrow">Next due</span><strong className="dateValue">{nextDue?fmtDate(nextDue):'No eligible obligation'}</strong><span className="muted">2025/26 onward only</span></div></div><div className="two"><section className="panel" id="connection"><h2>HMRC connection</h2><p className="muted">Government Gateway credentials stay with HMRC.</p><div className="detailGrid"><div><span className="eyebrow">NINO</span><strong>{taxpayer.nino||'Not saved yet'}</strong></div><div><span className="eyebrow">MTD Income Tax ID</span><strong>{taxpayer.mtditid||'Not saved yet'}</strong></div></div><div className={connectionStatus.usable?'status':'status statusError'}>{connectionStatus.detail}</div><Link className="btn" href={`/api/hmrc/oauth/start?taxpayerId=${encodeURIComponent(id)}`}>{connectionStatus.connected?'Reconnect to HMRC':'Connect to HMRC'}</Link></section><section className="panel" id="sync"><h2>Synchronise HMRC</h2><p className="muted">Refresh businesses and MTD obligations from HMRC sandbox.</p><form method="post" action="/api/hmrc/sync"><input type="hidden" name="taxpayerId" value={id}/><label>NINO</label><input className="field" name="nino" defaultValue={taxpayer.nino||''} required/><label>MTD Income Tax ID</label><input className="field" name="mtditid" defaultValue={taxpayer.mtditid||''} required/><button className="btn" type="submit">Synchronise now</button></form></section></div><div className={latestError?'status statusError':'status'}>Synchronisation: {syncStatus}{lastSync?` · Latest attempt ${new Date(lastSync).toLocaleString('en-GB')}`:''}{lastSuccessfulSync&&syncStatus!=='Complete'?` · Last successful sync ${new Date(lastSuccessfulSync).toLocaleString('en-GB')}`:''}{latestError?` · ${latestError}`:''}</div>{needsMatchingGrant&&<div className="status statusError">HMRC rejected the current token for this NINO. Reconnect this taxpayer using the sandbox test user that owns NINO {taxpayer.nino||'shown above'}, then run Synchronise now again.</div>}{acceptedPending.length>0&&<div className="status">{acceptedPending.length} quarterly update{acceptedPending.length===1?' has':'s have'} been accepted by HMRC while the obligation remains open. Synchronise HMRC to refresh the obligation status. These submissions are not counted as open to submit again.</div>}{legacyOnly&&<div className="status statusError"><strong>Legacy sandbox data only.</strong> HMRC returned {obligations.length} obligations from before 6 April 2025 for this test user. They are retained for connectivity diagnostics but are hidden from the modern MTD obligation selector and filing table.</div>}<section className="panel" style={{marginBottom:16}}><div className="sectionHead"><div><h2>HMRC business income sources</h2><p className="muted">Businesses returned by HMRC for this taxpayer.</p></div><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><Link className="btn btnSmall" href={`/taxpayers/${id}/businesses`}>Manage income sources</Link><Link className="btn btnSmall" href={`/taxpayers/${id}/quarterly`}>Quarterly obligations</Link></div></div><div className="tableWrap"><table><thead><tr><th>Type</th><th>Name</th><th>Business ID</th></tr></thead><tbody>{businesses.length?businesses.map(b=><tr key={b.id}><td>{b.business_type||'Self employment'}</td><td>{b.business_name||'Unnamed business'}</td><td className="mono">{b.business_id||''}</td></tr>):<tr><td colSpan={3} className="empty">No business income sources synchronised yet.</td></tr>}</tbody></table></div></section><section className="panel" id="obligations"><div className="sectionHead"><div><h2>MTD obligations</h2><p className="muted">Eligible quarterly obligation periods from tax year 2025/26 onward. The same period can appear more than once when HMRC returns separate income source IDs.</p></div>{eligibleYears.length>0?<form method="get" className="filterForm"><label htmlFor="year">Tax year</label><select id="year" name="year" defaultValue={selectedYear} className="selectField"><option value="all">All eligible tax years</option>{eligibleYears.map(y=><option key={y} value={y}>{y}</option>)}</select><button className="btn btnSmall" type="submit">Apply</button></form>:<div className="filterForm"><label>Tax year</label><select className="selectField" disabled defaultValue="none"><option value="none">No eligible tax years</option></select></div>}</div><div style={{display:'grid',gap:12}}>{obligationGroups.length?obligationGroups.map((group:any,index:number)=><details key={group.businessId} className="panel" style={{padding:0,overflow:'hidden'}} open={index===0}><summary style={{cursor:'pointer',padding:18,listStylePosition:'inside'}}><div style={{display:'flex',justifyContent:'space-between',gap:16,alignItems:'center',flexWrap:'wrap'}}><div><strong style={{fontSize:18}}>{group.label}</strong>{group.businessName&&<div className="muted">{group.businessName}</div>}<div className="muted"><span className="mono">{group.businessId==='not-supplied'?'HMRC income source ID not supplied':group.businessId}</span></div></div><div style={{display:'flex',gap:8,flexWrap:'wrap',alignItems:'center'}}><span className="statusPill statusOpen">{group.open} open</span><span className="statusPill statusDone">{group.fulfilled} fulfilled</span><span className="statusPill">{group.total} total</span><span className="muted"><strong>Next due:</strong> {group.nextDue?fmtDate(group.nextDue):'No open obligation'}</span></div></div></summary><div className="tableWrap"><table><thead><tr><th>Tax year</th><th>Period</th><th>Due</th><th>Status</th><th>Action</th></tr></thead><tbody>{group.rows.map((o:any)=>{const submission=acceptedSubmissionFor(submissions,o);const st=obligationStatus(o,submission);const taxYear=taxYearFor(o.period_start)||'';const query=new URLSearchParams({taxpayerId:id,businessId:group.businessId==='not-supplied'?'':group.businessId,periodStart:o.period_start||'',periodEnd:o.period_end||'',dueDate:o.due_date||'',taxYear,sourceType:group.sourceType});const canStart=String(o.status||'').toLowerCase()==='open'&&!submission;const actionHref=canStart?`/taxpayers/${encodeURIComponent(id)}/quarterly?${query.toString()}`:`/taxpayers/${encodeURIComponent(id)}/quarterly/history?${query.toString()}`;const actionLabel=canStart?'Start update':'View update';return <tr key={o.id} style={{cursor:'pointer',position:'relative'}}><td><Link href={actionHref} aria-label={`${actionLabel} for ${group.label}, period ending ${o.period_end||''}`} style={{position:'absolute',inset:0,zIndex:1}}/>{taxYear}</td><td>{fmtDate(o.period_start)} to {fmtDate(o.period_end)}</td><td>{fmtDate(o.due_date)}</td><td><span className={`statusPill ${st.cls}`}>{st.label}</span></td><td><Link className="btn btnSmall" href={actionHref} style={{position:'relative',zIndex:2}}>{actionLabel}</Link></td></tr>})}</tbody></table></div></details>):<div className="empty">No eligible MTD obligations from 2025/26 onward are available for this taxpayer.</div>}</div></section></main></div>
}
